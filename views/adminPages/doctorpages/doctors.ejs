<h1 class="text-center bg-dark text-info my-3 p-4 
rounded w-40 mx-auto">All Doctors</h1>
<div class="d-flex flex-row-reverse">
    <a class="btn text-info btn-outline-dark justify-content-end" href="/admin/addDoc" role="button">Add Doctor</a></div>
    
<div class="events-list my-3 text-center">
    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <!-- <th>Id</th> -->
                <th> Name</th>
                <th>username</th>
                <!-- <th>password</th> -->
                <!-- <th>Subjects</th> -->
                <th>actions</th>
            </tr>
        </thead>
        <% if(doctor.length> 0){%>
            <tbody>
                <%for(let doc of doctor){%>
                    <tr>
                        <!-- <th>
                            <%= doc.id%>
                        </th> -->
                        <th>
                            <%= doc.DOC_name%>
                        </th>
                        <th>
                            <%= doc.DOC_username%>
                        </th>
                        <!-- <th>
                            <%= doc.DOC_password%>
                        </th> -->
                        <!-- <th>
                            <%= doc.DOC_sub%>
                        </th> -->
                       
                        <th>
                            <a href="/admin/<%=doc.id%>/editDoc" class="mx-1 btn btn-success">Updata</a>
                            <a href="/admin/<%=doc.id%>/deleteDoc" class="mx-1 btn btn-danger">Delete</a>
                          
                        </th>
                    </tr>
                    <%}%>
            </tbody>
            <%}%>
    </table>
</div>
